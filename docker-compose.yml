version: '3.8'
services:
  biomekit:
    build: .
    volumes:
      - ./:/app
    environment:
      - DISPLAY=host.docker.internal:0.0
    network_mode: host

  r-studio:
    image: rocker/tidyverse:4.3
    ports:
      - "8787:8787"
    volumes:
      - ./R:/home/rstudio/R